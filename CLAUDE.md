# 특별실 예약 시스템 (ClassReserv)

> 학교 특별실(컴퓨터실 / AI실) 주간 예약 및 현황판 웹앱
> 이 문서는 **구현 전 요구사항 정의서**입니다. 코드 작성은 이 문서 확정 후 시작합니다.
> 최종 갱신: 2026-08-19

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 목적 | 교사가 특별실을 주 단위로 예약하고, 전체 사용 현황을 한눈에 확인 |
| 대상 특별실 | 컴퓨터실, AI실 (추후 관리자 화면에서 추가 가능하도록 설계) |
| 주요 사용자 | 일반 교사(예약자), 관리자(정보부/담당교사), 열람자(현황판) |
| 예약 단위 | `특별실 × 날짜 × 교시` 1칸 = 1예약 |
| 핵심 화면 | ① 주간 예약 시간표 ② 현황판(읽기 전용) ③ 관리자 페이지 |

### 확정된 결정 사항 (2026-08-19)

| 항목 | 결정 |
|---|---|
| 교사 명단 | **저장하지 않음** — 개인정보 최소화. 예약자가 이름을 직접 입력 |
| 예약 페이지 접근 | **접속 코드 없음** (누구나 접근 가능) |
| 예약 수정/취소 보호 | **예약할 때 본인이 정한 4자리 PIN** 으로만 수정·취소 |
| 관리자 | 관리자 페이지만 **별도 비밀번호** |
| 현황판 이름 표시 | **가운데 글자 마스킹** (예: 김철수 → 김O수) |
| 현황판 주소 | **공개 주소 `/board`** (검색엔진 색인만 차단) |
| 저장/배포 | Supabase(PostgreSQL) + Vercel |
| 표시 요일 | 월~금 (토·일 미표시) |
| 학년·반 | 드롭다운. **1·2·3학년 각 4반** |
| 범위 제외 | 반복 예약(N주 연속), 알림 발송, 토·일 예약 |

---

## 2. 개인정보 보호 원칙 ⭐

교사 이름 + 시간 + 장소가 결합되면 단순 이름보다 민감해지므로, 아래를 **설계 제약**으로 삼는다.

**수집 최소화**
- 교사 명단(전교 교사 목록) 테이블을 **만들지 않는다**
- 예약 1건에 저장하는 개인 식별 정보는 **예약자가 스스로 입력한 이름 1개**뿐
- 이메일·연락처·직위·학번 등 **수집하지 않는다**
- "필요 및 준비사항" 입력창에 **"학생 이름 등 개인정보는 적지 마세요"** 안내 문구 상시 노출

**노출 최소화**
- 현황판(`/board`)은 로그인 없이 열리므로 **마스킹된 이름만** 표시
- **"필요 및 준비사항"은 현황판에 표시하지 않는다** (관리자 페이지에서만 열람)
- `robots.txt` + `noindex` 메타태그로 검색엔진 색인 차단
- 페이지 하단에 "본 화면은 교내 특별실 운영 목적으로만 사용합니다" 안내

**이름 마스킹 규칙**

| 글자 수 | 규칙 | 예시 |
|---|---|---|
| 2글자 | 마지막 글자 마스킹 | 김수 → 김O |
| 3글자 | 가운데 글자 마스킹 | 김철수 → 김O수 |
| 4글자 이상 | 첫·끝만 남기고 가운데 전부 마스킹 | 남궁철수 → 남OO수 |
| 1글자·영문 등 | 그대로 표시 | - |

- 마스킹은 **서버(DB 뷰)에서 수행**한다. 원본 이름을 클라이언트로 내려보낸 뒤 화면에서만 가리는 방식은 금지 — 개발자 도구로 다 보인다.

**기술적 보호**
- Supabase **RLS 필수**. 익명(anon) 키로는 `reservations_public` **마스킹 뷰만** 읽을 수 있게 하고, 원본 테이블 직접 접근은 차단
- 모든 쓰기(생성·수정·삭제)는 서버 라우트에서 서비스 키로만 수행. 서비스 키는 클라이언트 번들에 절대 포함 금지
- PIN은 bcrypt 해시로만 저장, 평문·로그 기록 금지

**보존 기간**
- 예약 기록은 **학년도 종료 후 이름·준비사항을 자동 익명화**(예: 이름 → `(익명)`), 통계용 나머지 필드만 보존
- 관리자 페이지에서 기간 지정 일괄 삭제 가능

---

## 3. 사용자 역할 (Role)

| 역할 | 권한 |
|---|---|
| **교사(예약자)** | 주간 시간표 열람, 빈 칸 예약, **본인이 설정한 PIN 으로** 예약 수정/취소 |
| **관리자** | 비밀번호 로그인 후 모든 예약 강제 수정·삭제, 고정 점유 설정, 특별실·교시·드롭다운 항목 관리, 예약 정책 설정, 데이터 내보내기 |
| **열람자** | 현황판만 열람 (로그인 없음, 읽기 전용) |

### 인증·본인 확인 상세

- 예약 페이지 진입 시 **로그인 없음** — 바로 시간표가 보이고 빈 칸 클릭 시 예약
- 예약 저장 시 **이름 직접 입력 + 4자리 PIN 설정** (PIN은 해당 예약 1건 전용)
- 내 예약 수정·취소 시 그 PIN 입력 → 일치해야 진행
- PIN 5회 연속 오류 시 해당 예약에 대해 10분 잠금
- **PIN 분실 시**: 관리자에게 요청 → 관리자 페이지에서 강제 수정·삭제
- 편의 기능: 마지막에 입력한 이름을 **브라우저 로컬(localStorage)에만** 저장해 다음 예약 시 자동 채움 (서버로 전송·저장하지 않음)
- 관리자 로그인: `/admin` 접근 시 비밀번호 1개. bcrypt 해시를 환경변수 또는 `admin_config` 테이블에 저장, 세션은 HttpOnly 쿠키(8시간)

---

## 4. 교시 시간표 (확정)

| 교시 | 시간 | 비고 |
|---|---|---|
| 1교시 | 08:50 ~ 09:35 | |
| 2교시 | 09:45 ~ 10:30 | |
| 3교시 | 10:40 ~ 11:25 | |
| 4교시 | 11:35 ~ 12:20 | 오전 종료 |
| 점심 | 12:20 ~ 13:20 | 예약 대상 아님 (시간표에 구분선으로 표시) |
| 5교시 | 13:20 ~ 14:05 | |
| 6교시 | 14:15 ~ 15:00 | |
| 7교시 | 15:10 ~ 15:55 | 화·목만 |

- 수업 45분 + 쉬는 시간 10분 기준
- **요일별 교시 수**: 월 6, 화 7, 수 6, 목 7, 금 6
- 해당 요일에 없는 교시 칸(예: 월요일 7교시)은 **비활성 회색 처리**, 클릭 불가
- 교시 시각·요일별 교시 수는 모두 **관리자 페이지에서 수정 가능**한 설정값으로 저장

---

## 5. 화면별 기능 요구사항

### 5.1 주간 예약 시간표 (`/` 또는 `/reserve`)

**레이아웃**

```
[컴퓨터실] [AI실]  ← 특별실 탭        ◀ 2026-08-17 ~ 08-21 (8월 3주) ▶  [이번 주]

        |  월 8/17  |  화 8/18  |  수 8/19  |  목 8/20  |  금 8/21
--------+-----------+-----------+-----------+-----------+-----------
 1교시  | [고정] 정보|           | 국어 김O수 |          |
 2교시  |           | 영어 이O영|           | [고정] 정보|
 3교시  |           |           |           |           |   ← 클릭 시 예약 모달
 4교시  |           |           |           |           |
------------------ 점심시간 12:20~13:20 -------------------------
 5교시  |           |           |           |           |
 6교시  |           |           |           |           |
 7교시  |    ///    |           |    ///    |           |    ///     ← 해당 요일 없음
```

- 상단: 특별실 탭(컴퓨터실 / AI실) + 주간 네비게이션(◀ 이전 주 / 다음 주 ▶ / 이번 주)
- 셀 상태 5종
  - **빈 칸(예약 가능)**: 클릭 → 예약 모달
  - **예약됨**: 과목/이름(마스킹)/학년·반 표시. 클릭 → 상세 + `수정·취소(PIN 입력)` 버튼
  - **고정 점유**: 잠금 아이콘, 예약 불가 (관리자만 해제/변경)
  - **비활성**: 해당 요일에 없는 교시 / 휴업일 / 지난 시간
  - **내가 방금 만든 예약**: 이 브라우저에서 생성한 예약은 강조 표시(로컬 저장 기준, 서버 판단 아님)
- 예약 페이지에서도 이름은 **마스킹 표시**를 기본으로 한다 (원본 이름은 서버 밖으로 내보내지 않음)
- 모바일: 하루씩 보기(요일 스와이프) 또는 세로 리스트로 전환

**예약 모달 입력 항목**

| 필드 | 형식 | 필수 | 비고 |
|---|---|---|---|
| 특별실 | 자동 채움(읽기 전용) | O | 클릭한 탭 기준 |
| 날짜 / 교시 | 자동 채움(읽기 전용) | O | 클릭한 셀 기준 |
| 과목 | **드롭다운** | O | 국어/수학/영어/사회/과학/정보/기술가정/미술/음악/체육/도덕/창의적체험활동/자유학기/방과후/기타 (관리자 편집 가능) |
| **교사명** | **직접 입력**(텍스트, 2~10자) | O | 명단 없음. 마지막 입력값은 브라우저에만 기억 |
| 학년 | **드롭다운** | O | 1학년 / 2학년 / 3학년 / 전학년 / 해당없음 |
| 반 | **드롭다운** | O | **1~4반** + `전체`, `해당없음`. 학년별 반 수는 관리자 설정(초기값 각 4반) |
| 활용기기 | **드롭다운** | O | 데스크톱PC / 노트북 / 태블릿 / VR기기 / AI학습도구 / 3D프린터 / 기기 미사용 / 기타 (특별실별 목록 지정 가능) |
| 필요 및 준비사항 | 여러 줄 텍스트 (최대 500자) | - | 현황판 비노출. "학생 개인정보 입력 금지" 안내 문구 표시 |
| **수정용 PIN** | 숫자 4자리 (입력 마스킹) | O | 수정·취소 시 필요. 분실 시 관리자 문의 안내 |

**동작 규칙**
- 저장 시 `특별실+날짜+교시` 중복 검사 → 이미 예약/고정 점유면 저장 거부 및 사유 안내
- 동시 저장 충돌 방지: DB 유니크 제약 + 저장 실패 시 화면 자동 갱신
- 지난 날짜/교시, 휴업일, 해당 요일에 없는 교시는 예약·수정 불가 (관리자는 예외 허용)
- 같은 이름·PIN 반복 입력을 이용한 대량 등록 방지: IP 기준 분당 요청 제한(레이트 리밋)

### 5.2 현황판 대시보드 (`/board`) — 읽기 전용, 공개 주소

- 로그인 없이 접근, **모든 편집 UI 제거**, `noindex` + `robots.txt` 로 검색 차단
- 표시 내용
  - 좌우 2분할 또는 상하 배치로 **컴퓨터실 / AI실 동시 표시**
  - 기본 보기: **오늘** (탭으로 "이번 주" 전환)
  - 현재 시각 기준 **진행 중인 교시 하이라이트**, 다음 교시 안내 (12:20~13:20 은 "점심시간" 표시)
  - 각 칸: 과목 / **마스킹된 교사명(김O수)** / 학년·반 / 활용기기
  - **준비사항은 표시하지 않음**
- 복도 TV·전자칠판용 **큰 글씨 모드**, 다크 모드
- 60초 자동 새로고침(Supabase Realtime 구독), 상단에 "최종 갱신 시각" 표시
- 인쇄/PDF 저장 버튼 (주간 시간표 A4 가로 1장)

### 5.3 관리자 페이지 (`/admin`) — 비밀번호 필요

1. **고정 점유 관리** (핵심)
   - `특별실 + 요일 + 교시 + 라벨(예: 정보 3-1 정규수업)` 로 반복 점유 등록
   - 적용 기간: 시작일 ~ 종료일 (예: 2026-03-02 ~ 2026-07-17 = 1학기 전체)
   - 예외 처리: 특정 날짜만 점유 해제(공휴일·시험 기간 등)
   - 일괄 등록(요일·교시 다중 선택) 및 CSV 붙여넣기 가져오기
2. **특별실 관리**: 추가/수정/비활성화, 이름·위치·수용 인원·해당 실 전용 기기 목록
3. **선택 목록 관리**: 과목, 활용기기, 학년별 반 수 CRUD (순서 조정 포함)
4. **예약 정책 설정**
   - 예약 가능 범위 (기본: 이번 주 + 향후 3주)
   - 학기 기간, 휴업일/공휴일 등록(해당일 예약 차단)
   - 교시 시각 및 요일별 교시 수 편집 (§4 값이 초기값)
   - 관리자 비밀번호 변경
5. **예약 현황 관리**: 전체 예약 목록(필터: 기간/특별실/이름/과목/학년), **원본 이름·준비사항 열람**, 강제 수정·삭제, **엑셀(CSV) 내보내기**
6. **개인정보 관리**: 기간 지정 **일괄 익명화 / 삭제**, 학년도 종료 자동 익명화 설정
7. **변경 이력(감사 로그)**: 언제 어떤 예약이 생성/수정/삭제됐는지 기록 (관리자 조치 포함)

---

## 6. 데이터 모델 (초안)

```
rooms            id, name, code, location, capacity, is_active, sort_order
periods          id, no(1~7), label, start_time, end_time, is_active
weekday_periods  weekday(1~5), max_period          <- 월6/화7/수6/목7/금6
subjects         id, name, sort_order, is_active
devices          id, name, room_id(nullable=공통), sort_order, is_active
class_config     grade(1~3), class_count           <- 초기값 각 4반

reservations     id, room_id, date(YYYY-MM-DD), period_no,
                 subject, teacher_name(직접 입력 원본),
                 grade, class_no, device, notes(준비사항),
                 pin_hash, failed_count, locked_until,
                 status('active'|'cancelled'), is_anonymized,
                 created_at, updated_at
                 UNIQUE(room_id, date, period_no) WHERE status='active'

reservations_public   <- VIEW: 익명 접근 전용
                 room_id, date, period_no, subject,
                 masked_name(김O수), grade, class_no, device
                 (teacher_name 원본·notes·pin_hash 미포함)

fixed_blocks     id, room_id, weekday(1~5), period_no, label, teacher_label,
                 start_date, end_date, is_active
fixed_exceptions id, fixed_block_id, date          <- 특정 날짜만 점유 해제
holidays         id, date, name                    <- 예약 차단일
settings         key, value                        <- 예약 가능 주 수, 학기, 익명화 정책 등
admin_config     id, password_hash, updated_at
audit_logs       id, action, target_type, target_id, actor('teacher'|'admin'),
                 summary, created_at               <- 원본 이름은 기록하지 않음
```

**설계 포인트**
- 예약은 주(week)가 아니라 **실제 날짜(date)** 로 저장 → 주간 뷰는 조회 시 월~금 범위로 필터
- `fixed_blocks` 는 실제 예약 행을 미리 만들지 않고 **조회 시 합성(merge)** → 기간 변경 시에도 정합성 유지
- **마스킹은 DB 뷰(`reservations_public`)에서 처리**, 익명 키에는 이 뷰만 SELECT 권한 부여
- 익명화 처리 시 `teacher_name='(익명)'`, `notes=NULL`, `is_anonymized=true` 로 갱신

---

## 7. 기술 스택 (확정)

| 구분 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | **Next.js (App Router) + TypeScript** | 화면 3개 + API 를 한 프로젝트로 해결 |
| UI | Tailwind CSS + shadcn/ui | 표 형태 UI·모달 구성, 반응형 |
| DB | **Supabase (PostgreSQL)** | 무료 티어, 유니크 제약으로 중복 예약 차단, Realtime 으로 현황판 자동 갱신 |
| 인증 | 자체 구현 (예약별 PIN + 관리자 비밀번호, bcrypt) | 계정·명단 없이 최소 정보로 운영 |
| 배포 | Vercel | Git 푸시 시 자동 배포, HTTPS 기본 |

---

## 8. 비즈니스 규칙

1. 한 특별실의 한 교시에는 **예약 1건만** 가능 (DB 레벨 제약)
2. 고정 점유 시간은 예약 불가, 화면에 사유 라벨 표시
3. 예약 가능 범위: 기본 **이번 주 + 향후 3주**, 관리자 설정으로 변경
4. 지난 시간 예약·수정 불가 (관리자 예외)
5. 취소는 삭제가 아닌 `status='cancelled'` 처리 → 통계·이력 보존
6. 휴업일/공휴일은 예약 차단, 시간표에 회색 처리
7. 주 시작 = **월요일**, 표시 = 월~금 고정
8. 요일별 교시 수를 초과하는 칸은 존재하지 않는 것으로 처리
9. 원본 이름과 준비사항은 **관리자만** 볼 수 있다

---

## 9. 개발 단계 (마일스톤)

- ✅ **M0 · 준비**: 요구사항 확정(본 문서)
- ✅ **M1 · 핵심 예약**: DB 스키마 + RLS + 마스킹 뷰 → 주간 시간표 화면 → 예약 생성/수정/취소(PIN) → 중복 방지
- ✅ **M2 · 현황판**: `/board` 읽기 전용 화면, 자동 갱신, TV 모드, 인쇄, noindex
- ✅ **M3 · 관리자**: 고정 점유(+날짜별 예외), 휴업일, 예약 강제 취소, CSV 내보내기, 익명화
- ⬜ **M4 · 마감**: 모바일 대응 다듬기, 접근성, Supabase 연결, 실사용 테스트 및 배포

### 구현 시 확정한 사항 (문서 초안과 다른 부분)

| 항목 | 실제 구현 | 이유 |
|---|---|---|
| UI 라이브러리 | shadcn/ui 대신 **Tailwind 직접 작성** | 화면이 3개뿐이라 의존성을 늘릴 이유가 없음 |
| 로컬 개발 저장소 | Supabase 환경변수가 없으면 **`.data/dev.json` 파일**로 동작 | Supabase 없이도 바로 실행·검증 가능 |
| 관리자 비밀번호 | `ADMIN_PASSWORD` 환경변수. 미설정 시 **개발 환경에서만** 기본값 `admin1234` | 운영(NODE_ENV=production)에서는 미설정 시 로그인 자체를 차단 |
| 예약 칸 이동 | 수정 시 특별실·날짜·교시 변경 불가 (취소 후 재예약) | 칸 이동을 허용하면 중복 검사·PIN 확인이 복잡해짐 |
| 감사 로그 | 테이블만 정의, 기록 로직은 M4로 미룸 | 우선순위 조정 |

---

## 10. 남은 확인 사항

1. 컴퓨터실·AI실 각각의 **실제 보유 기기 목록** (현재는 일반적인 예시로 채워둠)
2. **관리자 비밀번호** (구현 후 첫 실행 시 설정해도 됨)
3. 특별실 추가 예정 여부(과학실·음악실 등)
4. 학교 개인정보 담당(정보부장/개인정보보호책임자)에게 **외부 클라우드(Supabase/Vercel) 사용 사전 협의**가 필요한지 확인 권장
5. 향후 필요 시 검토(현재 범위 제외): 반복 예약(N주 연속), 알림 발송, 토·일 예약

---

## 11. 코딩 규칙 (구현 시작 후 적용)

- 언어: TypeScript strict, 함수형 컴포넌트
- 날짜: `date-fns` 사용, 저장은 `YYYY-MM-DD` 문자열 + 타임존 `Asia/Seoul` 고정
- UI 문구는 한국어, 코드 식별자는 영어
- 중복 예약 검증은 **반드시 서버(DB 제약)에서** 수행, 클라이언트 검증은 보조 수단
- 이름 마스킹은 **DB 뷰에서** 처리, 원본 이름을 익명 클라이언트로 전송 금지
- PIN·서비스 키·세션 값은 클라이언트 번들·로그에 노출 금지
- 커밋 전 `npm run build` 및 타입 체크 통과 확인

---

## 12. 부록 — 한글(HWP/HWPX) 문서 작업

예약 현황을 한글 문서로 내보내는 등 `.hwp` / `.hwpx` 를 다뤄야 할 때는
**직접 구현하지 말고** 아래 전용 스킬을 사용한다.

- 저장소: https://github.com/Engccer/hwpx-automation
- 사용: 저장소를 clone 한 뒤 `SKILL.md` 를 따른다.
  환경 점검은 `python hwpx_edit.py --check-env`

제공 기능: HWP→HWPX 변환(hwplib/hwpxlib, 서식 보존), HWPX→Markdown,
MD/DOCX/HTML→HWPX, 표 셀 편집·텍스트 치환, 서명·도장 삽입, PDF 변환.
문서를 처음부터 만드는 방법은 `reference/build-from-scratch.md` 에 있다.

> OWPML 작성기를 새로 짜지 말 것. 위 스킬에 이미 있다.
